name: media_app
description: "A professional streaming and offline media application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  just_audio: ^0.9.36
  just_audio_background: ^0.0.1-beta.11
  dio: ^5.4.1
  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.1
  audio_session: ^0.1.18

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
