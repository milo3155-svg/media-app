name: media_app
description: "A complete raw video and background audio streaming app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  video_player: ^2.8.6
  youtube_explode_dart: ^2.2.2
  just_audio: ^0.9.36
  just_audio_background: ^0.0.1-beta.13
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
